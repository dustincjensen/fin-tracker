# fin-tracker
A financial tracker.

## Development

Run `npm run dev` to start development mode. This:
1. Cleans the previous build
2. Rebuilds TypeScript and Parcel in watch mode
3. Waits for the build output before launching Electron