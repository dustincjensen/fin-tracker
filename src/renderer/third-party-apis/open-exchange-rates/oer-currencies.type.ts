/**
 * The names of the currencies keyed by the 3 letter country code.
 * USD = United States Dollar
 * CAD = Canadian Dollar
 * EUR = Euro
 * GBP = British Pound Sterling
 */
export type OpenExchangeRatesCurrencies = Record<string, string>;